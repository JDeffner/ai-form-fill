/**
 * `ai-form-fill/react` — the `useFormFill` hook.
 * Separate entry point: `react` is an optional peer dependency and the core
 * bundle carries none of this code.
 */

export { useFormFill, type UseFormFillOptions, type UseFormFillResult } from './use-form-fill';
