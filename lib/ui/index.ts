/**
 * `ai-form-fill/ui` — the `<ai-form-fill>` custom element.
 * Separate entry point: importing the core pulls in no markup and no styles.
 */

export {
  AIFormFillElement,
  DEFAULT_STRINGS,
  defineFormFillElement,
  type AIFormFillStrings,
} from './element';
